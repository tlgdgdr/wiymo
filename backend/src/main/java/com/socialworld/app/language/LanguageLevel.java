package com.socialworld.app.language;

/** CEFR levels plus NATIVE (reserved for LanguageType.NATIVE entries). */
public enum LanguageLevel {
    A1,
    A2,
    B1,
    B2,
    C1,
    C2,
    NATIVE
}
