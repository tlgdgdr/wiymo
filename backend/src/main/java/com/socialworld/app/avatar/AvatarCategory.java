package com.socialworld.app.avatar;

/**
 * Layer categories, listed in render order (bottom to top).
 * The mobile client stacks transparent PNGs in this order.
 */
public enum AvatarCategory {
    BODY,
    BOTTOM,
    SHOES,
    TOP,
    FACE,
    EYES,
    HAIR,
    ACCESSORY
}
